# Sample Libary

This is a sample library that exposes a single variable
as it's export, as a sample project for parsing doc-ts.

<TypeDoc project="sample-lib" name="my_export" />
<TypeDoc project="sample-lib" name="My_Type" />